import { render } from '@testing-library/react'
import React from 'react'
import React, {Component} from 'react'
import {apiKey} from '../credentials/keys'


export default ShowRecipes extends Component {
    
    render() {
        return(
            
        )
    }
}